services:
  web:
    build: .
    ports:
      - 9191:9191
      - 9192:9192
    extra_hosts:
      - "backend:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9191/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
